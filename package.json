{
  "name": "dmatek",
  "private": true,
  "workspaces": [
    "site",
    "matek-source/*",
    "packages/*"
  ],
  "scripts": {
    "dev:site": "npm run dev --workspace=site",
    "build:site": "npm run build --workspace=site"
  }
}
