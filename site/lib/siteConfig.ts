// Production config for the props the v3.4 prototype exposed as editor
// toggles (`accent`, `animate`, `ticker`, `adire`). Ship with the same
// defaults the design specifies; flip `adire` here if the Adire pattern
// is ever retired site-wide.

export const siteConfig = {
  adire: true,
};
